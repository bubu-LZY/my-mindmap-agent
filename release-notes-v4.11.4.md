# v4.11.4 更新日志

## 新增
- **run_code 工具白名单大幅扩充**：从 13 个扩展到 42 个，新增删除节点、插入父节点、合并节点、批量移动、重排序、挖空操作、样式操作、多种导出等能力
- **首轮工具预匹配**：用户发送消息后，本地先做一次关键词匹配，高分未激活工具自动激活，AI 第一轮就能看到可用工具

## 优化
- **系统 prompt 增加工具发现引导**：告诉 AI 不确定功能是否存在时，先调用 `semantic_tool_search` 搜索，不要直接说"做不到/不支持"
- **系统 prompt 增加 run_code 使用引导**：批量操作、多步骤逻辑、条件循环等场景优先用 `run_code` 写 JS 代码实现
- **run_code 只读代码自动放行**：自动分析代码中调用的工具，全为只读工具时无需用户确认，直接执行
- **自动工具发现触发条件放宽**：不只是"拒答"才触发，回复很短且无实质内容时也触发语义检索兜底
- **自动发现结果合并**：本地 n-gram 匹配 + 语义检索结果合并去重，命中率更高

## 修复
- 大纲模式展开所有节点后立即收起的问题（改为双向同步展开状态）

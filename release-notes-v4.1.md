## 4.1 更新内容

- 修复概要节点在切换标签或重启后丢失文字颜色、加粗、高亮等富文本样式的问题。
- 修复导图画布缩放状态未按文件记忆，切换标签后回到最小缩放的问题。
- 集成并适配小地图导航窗：默认关闭，左下角“地图”按钮展开；蓝框支持拖拽定位、框内滚轮调整框大小，并修正蓝框与缩略图的坐标对齐。
- 记事本改为后台静默自动保存，不再反复显示“已保存”。
- 修复记事本有序列表在空列表项回车时删除序号错位、无法继续回车的问题。
- 修复内置浏览器拖拽文件上传提示区域过大、落点与网页输入框不对齐的问题。
- 修复切换 AI 配置档后，对话界面模型列表仍显示旧配置档的问题。
- 修复 AI 智能挖空因节点业务 uid 与渲染 uid 不一致，导致 AI 返回结果全部匹配失败、退化为本地兜底的问题。
- 调整 AI 全文挖空调度：失败后先原样重试，仍失败再拆小批次重试一次，只有最终失败才本地兜底；不再因为单批失败直接把剩余批次全部降级。
- 优化多项 AI 提示词：压缩主系统提示的查询/文档部分，工具路由加入正反例，AI 挖空改为正向引导与内部推理，JSON 输出兼容代码块并增加空输入保护，摘要增加单段字数上限。

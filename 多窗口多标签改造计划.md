# 多窗口 + 多标签 + AI 防误改 改造计划（大白话版）

> 目标对应你那份 PRD 的三块：①AI 任务绑死目标文件防串台 ②多 Tab 标签 + 拖拽剥离独立窗口 ③目录树拖文件进 AI 对话框。
> 已确认的决策：同文件禁止多开（重复打开→提示+跳转）、冲突保护作废、Tab 常驻内存不关、剥离窗口时 AI 任务跟着标签走。

---

## 一、现在的坑到底在哪（先讲清楚根因）

整个程序现在只有"一个当前文件"这个概念。代码里有个全局仓库（`mindMapStore`），里面就一个思维导图实例、一个文件路径。所有地方都认这个"当前"：

- 你打开 A 文件 → 仓库里"当前"= A。
- AI 任务跑起来 → 它通过仓库拿"当前"来改，改的是 A。✅
- 你中途切到 B 文件 → 仓库里"当前"被换成 B。
- AI 任务还在跑，它又去仓库拿"当前" → 拿到的是 B → 改错文件了。❌

**根因一句话**：AI 任务没有"工牌"，它每次干活都现去问"当前是哪个文件"，而"当前"会被你随时换掉。

---

## 二、总思路（打个比方）

现在像"一台电视，只能放一个频道"。要改成：

- **多台电视**（多个独立窗口），每台都能看不同频道，还能摆一排并排看；
- **每台电视有节目单**（顶部 Tab 标签栏），一个标签 = 一个文件；
- **遥控器带工牌**（AI 任务绑死 fileId），遥控器只认它工牌上那台电视，绝不串台。

---

## 三、分两仗打

### 第一仗：先止血（小改，预计 1~2 天见效）

**目的**：在不动大架构的前提下，先把"AI 改错文件"这个最痛的 bug 堵住。你不用等大重构，很快就能用上。

**做什么（大白话）**：
1. 给每个 AI 任务发一张"工牌"——工牌上写死它发起时那个文件的路径（fileId）。
2. 任务从头到尾带着工牌，干活时先把工牌亮出来。
3. 工具层（AI 实际改节点的那堆函数）每动一下，先核对工牌：要改的节点不是工牌上这个文件的，一律拒绝。
4. 任务跑的时候，"当前文件"随你切，AI 只认工牌上那个文件。

**技术落点**：
- `src/services/toolHandler.js`：所有工具函数加 `fileId` 入参，内部按 `fileId` 从"实例表"里取对应思维导图，不再直接用全局"当前"。
- `src/services/aiService.js`：发起任务时把"当前文件路径"记成 taskId 的 fileId，全程透传。
- `src/stores/mindMapStore.js`：加一个"按 fileId 存实例"的表（暂不强求多实例，先让工具能按 fileId 找到对的那个）。

**改完你能看到**：AI 跑的时候你随便切文件、来回点，它只动它该动的那份，不再串台。

**风险**：低。改动集中在工具层和任务发起处，可单独验证，不影响你日常使用。

---

### 第二仗：大重构多窗口多标签（大工程，分 4 步）

这部分把"一个程序一个文件"彻底改成"多窗口 + 多标签"。

#### 第 1 步：顶部 Tab 标签栏（基础）
**做什么**：顶部加一排标签，打开文件 = 加标签，点标签切换文件。同一个文件禁止重开（再开就提示"已在某标签打开"并跳过去）。每个标签自己存视图状态（大纲/脑图、滚动位置、展开折叠）。标签常驻内存不关，切换秒切。

**技术落点**：新建 `TabBar.vue`；`App.vue` 把"单文件"改成"标签数组 + 当前激活标签"；`mindMapStore` 从单例改成"按 fileId 存多个实例"的注册表。

**改完能看到**：能同时打开好几个文件，标签来回切，每个标签状态独立保留。

#### 第 2 步：AI 任务与 Tab 绑定
**做什么**：AI 任务发起时绑"当前激活 Tab 的 fileId"（和第一仗接上）。切 Tab、开新 Tab 都不影响已发起的任务。关 Tab 时如果上面有 AI 在跑，弹"终止任务 / 取消关闭"。

**技术落点**：第一仗的 fileId 绑定从"单文件"扩到"当前 Tab"；关 Tab 前查任务表。

**改完能看到**：在 A 标签发任务，切到 B 标签干活，A 的任务照样只改 A。

#### 第 3 步：拖拽标签剥离成独立窗口（最大块）
**做什么**：把一个标签拖到窗口外 → 主进程新建一个独立窗口，把这个标签搬过去。新窗口是完整程序，功能一个不少。标签上若有 AI 任务，任务跟着搬到新窗口显示进度。反向：把独立窗口的标签拖回任意窗口标签栏 → 合并进去，独立窗口销毁。每个窗口最后一个标签不许拖出去。

**技术落点**：`electron/main.js` 多窗口管理（多 BrowserWindow）；新增标签跨窗口搬运的 IPC；`App.vue` 拖拽剥离/合并交互；窗口间通信全程用 fileId 路由。

**改完能看到**：拖出标签变独立窗口，两个脑图能并排对照看，各跑各的 AI 互不干扰。

#### 第 4 步：目录树拖文件进 AI 对话框 + 状态持久化
**做什么**：左侧目录树里的 `.smm` 文件能拖到 AI 输入框，生成 `@[文件名](fileId:xxx)` 标记；提交时以拖进来的 fileId 为准（优先于当前 Tab）。文件夹不让拖，拖了提示"只能拖思维导图文件"。退出程序时记下所有窗口位置大小 + 每个窗口的标签列表，重启自动恢复；恢复时某个文件已被删/挪走，就跳过那个标签并小声提示。

**技术落点**：`FileTree.vue` 加拖拽源；`ChatPanel.vue` 加放置区 + 标记解析；`electron-store` 存窗口/标签状态；`main.js` 启动时读回并恢复。

**改完能看到**：从目录树一拖就把文件指定给 AI；关掉重开，上次所有窗口和标签都原样回来。

---

## 四、收尾兜底

- 多标签常驻内存，加个上限保护（比如超过 8~10 个提示一下，避免卡）。
- AI 工具层加硬校验：工具调用没带 fileId 或 fileId 跟任务绑的对不上 → 一律拒绝执行。
- 多窗口所有 IPC 通信都用 fileId 当钥匙，谁也不许越界。

---

## 五、我建议的推进方式

1. **先打第一仗止血**（1~2 天）——你先用着，验证"AI 不再串台"。这块价值最高、风险最低、最快见效。
2. **再进第二仗**，按第 1→2→3→4 步顺序，**每步做完我先让你验，通过了再下一步**。其中第 3 步（多窗口）最重，可以等第 1、2 步（Tab + AI 绑定）让你看到甜头后再做。

---

## 六、要你拍板的地方

- 这个分两仗、第二仗分 4 步的节奏，你认可吗？
- 第 1 步（Tab 标签栏）和第 2 步（AI 绑 Tab）能不能先做，让你早一点看到"多文件 + AI 不串台"的效果？多窗口（第 3 步）放后面。
- 有没有哪块你觉得优先级要调（比如更想先要"目录树拖进 AI 框"）？

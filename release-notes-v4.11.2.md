# v4.11.2 更新日志

## 新增

- 新增文件关系图谱模块（`fileRelationGraph.js`），自动建立文件间引用关系与主题相似度
- 新增 `get_related_files` AI 工具，可查询单个文件的所有关联文件
- 新增 `search_across_files` AI 工具，跨文件搜索结果按文件分组，附带关联文件推荐
- 新增 `ask` AI 工具，支持 AI 主动向用户发起选择题确认
- 对话历史新增搜索功能，支持按标题和消息内容搜索
- 全局对话支持：历史记录可切换「当前文件/全局对话/全部」视图，可新建全局对话
- AI 对话头部新增返回按钮，工具调用跳转文件后可一键返回原文件

## 优化

- 多模态 PDF 上传：Files API 同批次失败一次后自动跳过后续文件，避免重复降级浪费时间
- 多模态无 tools 模式双保险：方案B（JSON 伪工具调用）+ 方案C（自动切回主模型）
- 方案B 提示词强化：明确禁止"我将调用"句式，增加正反例对比，提高 JSON 格式输出命中率
- 工具调用卡片新增实时进度显示，可看到当前执行步骤（如"正在解析教材 1/3"）
- 深度思考展开后自动滚动到底部，流式更新时持续滚动，无需收起再展开
- 生成导图跳转后自动居中到根节点
- 引用工具/技能弹出列表 z-index 从 20 提升至 200，避免被其他元素遮挡
- 云盘同步设置：移除 123 云盘，新增 TeraBox、Yandex Disk 选项，更新坚果云说明

## 修复

- 修复 AI 续写后节点重叠和 HTML 标签显示问题
- 修复 AI 全文挖空 removeChild 报错
- 修复挖空点击导致编辑态意外退出的问题
- 修复方向键导航视图跳动（视口内节点不再居中）
- 修复方向键偶发失效（增加 range 缓存兜底）
- 修复复制节点带 HTML 标签问题，改为纯文本复制
- 修复 ask 工具"未知工具"报错
- 修复多模态模式下 AI 任务中途中断的问题

## 4.11.1 更新内容

### 修复

- **引用命中支持 PDF 及其他文档类型**：此前把引用悬浮在 PDF / Word / Excel / PPT / CSV / 纯文本等文档上会提示「不支持的文件格式」。现在这类文档在悬浮预览中可直接查看内容（PDF 渲染首页缩略图、表格类展示表格、文本类展示正文），右上角按钮由「去编辑」改为「去查看」，点击后用文档查看器只读打开。
- **修复 Markdown 引用误报「暂不支持直接编辑」**：此前引用到 Markdown 文件时提示「请使用思维导图格式」。现在会智能判断——若存在同名 .smm（同目录或默认保存位置）直接打开那份导图编辑；若 Markdown 内容本身就是导图大纲（多级标题/嵌套列表），则自动还原为同名 .smm 并打开编辑（原 Markdown 保持不变）；普通 Markdown 才按只读文档查看。
- **引用文件列表按类型区分图标**：思维导图、PDF、Word、Excel、PPT、Markdown、纯文本等使用不同图标，一眼区分「可编辑」与「只读」。

### 优化

- 超大 PDF（>25MB）悬浮预览不再整文件解析，避免卡顿，提示改用文档查看器打开。

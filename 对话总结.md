# 思维导图 AI 助手 —— 对话工作总结

> 项目：Vue + Electron 思维导图应用（simple-mind-map）
> 整理时间：2026-08-21

## 一、AI 工具与稳定性

### 关键词匹配（解决"标出重要内容/加粗"失败）
- 新增 `normalizeForMatch`：匹配前去除 HTML、所有空白与零宽/不可见字符（U+FEFF、U+200B~U+200F、U+2060、软连字符等）。
- 解决节点文本中混入零宽空格/连续空格导致关键词 `includes` 匹配不到的问题。
- `batch_node_actions` 单次步骤上限 8 → 50，避免标多个重点被迫拆多次调用出错。
- 系统提示新增"后续修改复用历史 / 标重点批处理"规则。

### 一键整理框架工具 `reorganize_mindmap`
- 新增核心工具：AI 读取当前导图 → 重新梳理层级/归类/排序 → 保存为新文件 `主题【整理框架】.smm`（存到源导图目录）。
- 严格保留原文、只调结构，原导图不动；JSON 输出解析，`externalFile` 标记（不切换当前画布）。

### 新增子节点流程重构
- 提问顺序改为：① 参考资料 → ② 新增几个层级 → ③ 其他要求。
- 有参考资料时 prompt 强制"严格依据资料、不过度总结、不自由发散"。
- 新增规则：第一层级只生成 1 个节点，从第二层级开始延伸。

### 提问改写工作流
- 用户发送消息后、主 AI 执行前，先单独调用一次 AI 对问题进行意图识别与规范化改写。
- 改写结果以「【意图规范化改写】」附在原始问题后传给主 AI，原始问题始终保留；失败不阻断。

### 全文挖空（并行 + 进度 + 增量）
- `aiCloze.js`：由"全部算完再一次性应用"改为"每批结果一到就立刻写入节点"。
- 失败/停止时已完成部分保留并渲染，不再"白等后重来"。
- 进度升级为百分比：`已完成 X/Y 批（Z%）`；消息里 N 路子 Agent 卡片随进度逐个变完成。
- 保留 5~15 路并发、每批 12 节点、失败批次自动重试、限流自动降并发。

### 本地文件快速语义检索 `retrieve_local_file`
- 拖入 PDF/docx/txt 等大文件后，AI 优先用 `retrieve_local_file(file_path, query)` 只返回最相关片段，而非读全文。
- 文本提取带会话级缓存；按段落切片 + 中文 bigram + 词项命中打分；扫描版 PDF 明确提示走 OCR 兜底。

## 二、Bug 修复

- **生成导图首次失败**：`buildUidMap` 返回 `nodes` 而代码访问 `uidMap.list`，节点数 >200 时抛 `Cannot read properties of undefined (reading 'length')`，且文件已保存导致重试生成 `(1)` 副本。已改为 `uidMap.nodes.length` 并补 `list` 别名。
- **Markdown 导入解析**：重写 `markdownParser.js`，按缩进还原多级层级（自动检测 2/4 空格），并支持行内 `**粗体**`、`*斜体*`、`~~删除线~~`、`` `代码` ``；导出反向还原。
- **导入按钮图标方向**：`Upload` → `Download`（向下箭头 = 导入）。

## 三、界面 / 交互

- 目录树新增「导入文件」按钮，支持 XMind / Markdown。
- 右键空白区域菜单新增「一键整理导图（为新文件）」。
- 大纲模式 Shift+Enter 在当前节点内换行（插入 `<br>`）。
- 目录树重命名时禁用整树拖拽，可正常拖动选中文字。
- 最近打开按文件名去重；节点名悬浮显示完整文件名。
- 全文背诵改写（AI 全文背诵改写）功能已按需求移除。

## 四、打包产物

- `my-mindmap-agent-source.zip` — 源码
- `my-mindmap-agent-setup.zip` — exe 安装程序
- `release\my-mindmap agent Setup 1.0.0.exe`

## 五、待关注 / 建议

- 全文/大任务慢的底层瓶颈仍是 LLM 单次延迟与供应商并发上限，代码层已尽量并行 + 增量 + 进度。
- 弱模型（如 flash-lite）仍可能误解意图（例如"【】变蓝"被当作知识问答），建议关键任务用更稳模型（deepseek-v4-pro）。
- 旧的错误导入 `.smm` 需删除后重新导入，才能看到 Markdown 层级/加粗修复效果。

## 4.4 更新内容

- **工具注册制架构（ToolRegistry）**：重构工具系统为注册制模式，每个工具独立定义，支持统一的注册、发现、调用接口，方便后续扩展和维护。
- **统一超时控制**：所有工具按类型智能配置超时时间（AI操作2分钟、导出1分钟、网络操作30秒、导图生成类不设硬超时），防止工具异常卡死。
- **统一输出大小限制**：工具返回内容超过限制时自动截断并提示，避免超大输出撑爆AI上下文（普通工具2MB，大数据工具5MB）。
- **新增 run_code 代码执行工具**：支持在沙箱中执行 JavaScript 代码，可以调用所有已注册工具、访问导图实例，用于批量操作、复杂逻辑、数据处理等场景。执行前需用户确认，安全可控。
- 优化工具系统扩展性：新工具只需独立文件 + 注册，无需改动主流程代码。
